# Examples

Small, self-contained usage examples live here.

- `entity_resolution_test.py`: loads a Phase-2 run folder and evaluates a simple record-linkage baseline against the generated truth crosswalk
